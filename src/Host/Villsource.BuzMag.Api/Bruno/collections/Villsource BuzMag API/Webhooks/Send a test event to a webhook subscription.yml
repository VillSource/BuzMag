info:
  name: Send a test event to a webhook subscription
  type: http
  seq: 5
  tags:
    - Webhooks

http:
  method: POST
  url: "{{baseUrl}}/api/v1/webhooks/subscriptions/:id/test"
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/webhooks/subscriptions/:id/test"
      method: POST
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "success": false
          }
