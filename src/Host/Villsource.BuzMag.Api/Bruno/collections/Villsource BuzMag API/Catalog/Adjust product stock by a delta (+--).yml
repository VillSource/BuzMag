info:
  name: Adjust product stock by a delta (+/-)
  type: http
  seq: 24
  tags:
    - Catalog

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/catalog/products/:productId/stock"
  params:
    - name: productId
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "productId": "",
        "delta": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/catalog/products/:productId/stock"
      method: PATCH
      params:
        - name: productId
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "productId": "",
            "delta": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
