info:
  name: List soft-deleted brands
  type: http
  seq: 1
  tags:
    - Catalog

http:
  method: GET
  url: "{{baseUrl}}/api/v1/catalog/brands/trash"
  params:
    - name: pageNumber
      value: ""
      type: query
      disabled: true
    - name: pageSize
      value: ""
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/catalog/brands/trash"
      method: GET
      params:
        - name: pageNumber
          value: ""
          type: query
          disabled: true
        - name: pageSize
          value: ""
          type: query
          disabled: true
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
