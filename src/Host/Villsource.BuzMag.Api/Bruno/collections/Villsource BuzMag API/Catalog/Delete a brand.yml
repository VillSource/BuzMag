info:
  name: Delete a brand
  type: http
  seq: 6
  tags:
    - Catalog

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/catalog/brands/:brandId"
  params:
    - name: brandId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/catalog/brands/:brandId"
      method: DELETE
      params:
        - name: brandId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
