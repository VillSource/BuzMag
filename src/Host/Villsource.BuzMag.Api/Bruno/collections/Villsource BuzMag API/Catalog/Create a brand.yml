info:
  name: Create a brand
  type: http
  seq: 3
  tags:
    - Catalog

http:
  method: POST
  url: "{{baseUrl}}/api/v1/catalog/brands"
  body:
    type: json
    data: |-
      {
        "name": "",
        "description": "",
        "logoUrl": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/catalog/brands"
      method: POST
      body:
        type: json
        data: |-
          {
            "name": "",
            "description": "",
            "logoUrl": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
