info:
  name: Create a ticket
  type: http
  seq: 9
  tags:
    - Tickets

http:
  method: POST
  url: "{{baseUrl}}/api/v1/tickets"
  body:
    type: json
    data: |-
      {
        "title": "",
        "description": "",
        "priority": "Low",
        "assignedToUserId": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/tickets"
      method: POST
      body:
        type: json
        data: |-
          {
            "title": "",
            "description": "",
            "priority": "Low",
            "assignedToUserId": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
