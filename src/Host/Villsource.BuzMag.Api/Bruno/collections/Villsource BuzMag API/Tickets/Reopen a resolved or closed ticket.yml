info:
  name: Reopen a resolved or closed ticket
  type: http
  seq: 7
  tags:
    - Tickets

http:
  method: POST
  url: "{{baseUrl}}/api/v1/tickets/:ticketId/reopen"
  params:
    - name: ticketId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/tickets/:ticketId/reopen"
      method: POST
      params:
        - name: ticketId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
