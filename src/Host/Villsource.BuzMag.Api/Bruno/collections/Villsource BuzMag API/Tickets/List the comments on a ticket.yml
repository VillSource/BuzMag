info:
  name: List the comments on a ticket
  type: http
  seq: 3
  tags:
    - Tickets

http:
  method: GET
  url: "{{baseUrl}}/api/v1/tickets/:ticketId/comments"
  params:
    - name: ticketId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/tickets/:ticketId/comments"
      method: GET
      params:
        - name: ticketId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
