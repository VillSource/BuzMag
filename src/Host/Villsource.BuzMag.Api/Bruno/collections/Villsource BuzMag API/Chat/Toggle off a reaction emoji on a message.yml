info:
  name: Toggle off a reaction emoji on a message
  type: http
  seq: 21
  tags:
    - Chat

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/chat/messages/:id/reactions/:emoji"
  params:
    - name: id
      value: ""
      type: path
    - name: emoji
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/chat/messages/:id/reactions/:emoji"
      method: DELETE
      params:
        - name: id
          value: ""
          type: path
        - name: emoji
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
