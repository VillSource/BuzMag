info:
  name: Edit a message — author only
  type: http
  seq: 16
  tags:
    - Chat

http:
  method: PUT
  url: "{{baseUrl}}/api/v1/chat/messages/:id"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "body": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/chat/messages/:id"
      method: PUT
      params:
        - name: id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "body": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
