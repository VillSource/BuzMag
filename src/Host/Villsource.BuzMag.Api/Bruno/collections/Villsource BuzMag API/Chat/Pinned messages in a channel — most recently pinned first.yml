info:
  name: Pinned messages in a channel — most recently pinned first
  type: http
  seq: 15
  tags:
    - Chat

http:
  method: GET
  url: "{{baseUrl}}/api/v1/chat/channels/:id/pinned"
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/chat/channels/:id/pinned"
      method: GET
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
