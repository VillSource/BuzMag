info:
  name: Reset password
  type: http
  seq: 26
  tags:
    - Identity

http:
  method: POST
  url: "{{baseUrl}}/api/v1/identity/reset-password"
  headers:
    - name: tenant
      value: ""
  body:
    type: json
    data: |-
      {
        "email": "",
        "password": "",
        "token": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/identity/reset-password"
      method: POST
      headers:
        - name: tenant
          value: ""
      body:
        type: json
        data: |-
          {
            "email": "",
            "password": "",
            "token": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

docs: Reset the user's password using the provided verification token.
