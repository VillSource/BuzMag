info:
  name: Request password reset
  type: http
  seq: 25
  tags:
    - Identity

http:
  method: POST
  url: "{{baseUrl}}/api/v1/identity/forgot-password"
  headers:
    - name: tenant
      value: ""
  body:
    type: json
    data: |-
      {
        "email": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/identity/forgot-password"
      method: POST
      headers:
        - name: tenant
          value: ""
      body:
        type: json
        data: |-
          {
            "email": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

docs: Generate a password reset token and send it via email.
