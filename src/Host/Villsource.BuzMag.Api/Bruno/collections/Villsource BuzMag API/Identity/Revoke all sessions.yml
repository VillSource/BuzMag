info:
  name: Revoke all sessions
  type: http
  seq: 31
  tags:
    - Identity

http:
  method: POST
  url: "{{baseUrl}}/api/v1/identity/sessions/revoke-all"
  body:
    type: json
    data: "{}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/identity/sessions/revoke-all"
      method: POST
      body:
        type: json
        data: "{}"
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
  - name: 401 Response
    description: Unauthorized
    request:
      url: "{{baseUrl}}/api/v1/identity/sessions/revoke-all"
      method: POST
      body:
        type: json
        data: "{}"
    response:
      status: 401
      statusText: Unauthorized
      body:
        type: text
        data: ""
  - name: 403 Response
    description: Forbidden
    request:
      url: "{{baseUrl}}/api/v1/identity/sessions/revoke-all"
      method: POST
      body:
        type: json
        data: "{}"
    response:
      status: 403
      statusText: Forbidden
      body:
        type: text
        data: ""

docs: Revoke all sessions for the currently authenticated user except the current one.
