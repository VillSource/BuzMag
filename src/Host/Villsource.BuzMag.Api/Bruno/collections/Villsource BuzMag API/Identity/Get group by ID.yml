info:
  name: Get group by ID
  type: http
  seq: 38
  tags:
    - Identity

http:
  method: GET
  url: "{{baseUrl}}/api/v1/identity/groups/:id"
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/identity/groups/:id"
      method: GET
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "id": "",
            "name": "",
            "description": "",
            "isDefault": false,
            "isSystemGroup": false,
            "memberCount": "",
            "roleIds": "",
            "roleNames": "",
            "createdAt": ""
          }

docs: Retrieve a specific group by its ID including roles and member count.
