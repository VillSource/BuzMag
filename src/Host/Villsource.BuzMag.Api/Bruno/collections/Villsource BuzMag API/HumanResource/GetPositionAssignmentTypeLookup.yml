info:
  name: GetPositionAssignmentTypeLookup
  type: http
  seq: 16
  tags:
    - HumanResource
    - lookups

http:
  method: GET
  url: "{{baseUrl}}/api/v1/employees/position-assignment-type/lookup"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/employees/position-assignment-type/lookup"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "key": ""
            }
          ]
