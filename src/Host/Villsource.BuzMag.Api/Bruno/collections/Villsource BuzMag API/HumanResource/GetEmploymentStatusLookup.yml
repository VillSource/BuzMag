info:
  name: GetEmploymentStatusLookup
  type: http
  seq: 14
  tags:
    - HumanResource
    - lookups

http:
  method: GET
  url: "{{baseUrl}}/api/v1/employees/employment-status/lookup"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/employees/employment-status/lookup"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "key": ""
            }
          ]
