info:
  name: Void an invoice
  type: http
  seq: 14
  tags:
    - Billing

http:
  method: POST
  url: "{{baseUrl}}/api/v1/billing/invoices/:invoiceId/void"
  params:
    - name: invoiceId
      value: ""
      type: path
  body:
    type: json
    data: "{}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/billing/invoices/:invoiceId/void"
      method: POST
      params:
        - name: invoiceId
          value: ""
          type: path
      body:
        type: json
        data: "{}"
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
