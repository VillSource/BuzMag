info:
  name: List files uploaded by the current user
  type: http
  seq: 2
  tags:
    - Files

http:
  method: GET
  url: "{{baseUrl}}/api/v1/files/mine"
  params:
    - name: page
      value: ""
      type: query
      disabled: true
    - name: pageSize
      value: ""
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/files/mine"
      method: GET
      params:
        - name: page
          value: ""
          type: query
          disabled: true
        - name: pageSize
          value: ""
          type: query
          disabled: true
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
